// jar包的名字
archivesBaseName = 'cache-mongo'

dependencies{
    compile(
            //mongodb
            "org.springframework.data:spring-data-mongodb:1.10.7.RELEASE",
            "org.mongodb:mongo-java-driver:3.1.0"
    )
}